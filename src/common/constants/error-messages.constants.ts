/**
 * Default error messages used by the global exception filter
 */

/**
 * Default error message returned when no specific message is available
 */
export const DEFAULT_ERROR_MESSAGE = "Internal server error";

/**
 * Default error name/type returned when no specific error type is available
 */
export const DEFAULT_ERROR_NAME = "Internal Server Error";
